# Upstream Sync Spec: v2026.4.29

- Upstream release: https://github.com/openclaw/openclaw/releases/tag/v2026.4.29
- Tracking issue: https://github.com/a3tai/openclaw-go/issues/112

## Upstream Release Diff Evidence

- Compare range: `v2026.4.27..v2026.4.29`
- Upstream compare: https://github.com/openclaw/openclaw/compare/v2026.4.27...v2026.4.29
- Changed files: **2858**

### RPC method deltas

- Added methods: **0**
- Removed methods: **0**
- Methods touched in changed server-method files: **52**

### Notable upstream changes

Multiple server-method files were updated (agent, chat, cron, doctor, exec-approval, health, models, nodes, sessions). No new methods were introduced in this release delta, but twelve methods present in the upstream gateway list (`server-methods-list.ts` and `method-scopes.ts`) had not yet been implemented in openclaw-go.

## openclaw-go changes required

- [x] Add `diagnostics.stability` method (`DiagnosticsStability`) and `MethodDiagnosticsStability` constant
- [x] Add `channels.start` method (`ChannelsStart`), `ChannelsStartParams` type, and `MethodChannelsStart` constant
- [x] Add `tts.personas` method (`TTSPersonas`) and `MethodTTSPersonas` constant
- [x] Add `talk.realtime.session` method (`TalkRealtimeSession`) and `MethodTalkRealtimeSession` constant
- [x] Add `update.status` method (`UpdateStatus`) and `MethodUpdateStatus` constant
- [x] Add `voicewake.routing.get` method (`VoiceWakeRoutingGet`) and `MethodVoiceWakeRoutingGet` constant
- [x] Add `voicewake.routing.set` method (`VoiceWakeRoutingSet`), `VoiceWakeRoutingSetParams` type, and `MethodVoiceWakeRoutingSet` constant
- [x] Add `node.pair.remove` method (`NodePairRemove`), `NodePairRemoveParams` type, and `MethodNodePairRemove` constant
- [x] Add `push.web.subscribe` method (`PushWebSubscribe`), `PushWebSubscribeParams` type, and `MethodPushWebSubscribe` constant
- [x] Add `push.web.unsubscribe` method (`PushWebUnsubscribe`), `PushWebUnsubscribeParams` type, and `MethodPushWebUnsubscribe` constant
- [x] Add `push.web.test` method (`PushWebTest`), `PushWebTestParams` type, and `MethodPushWebTest` constant
- [x] Add `assistant.media.get` method (`AssistantMediaGet`) and `MethodAssistantMediaGet` constant
- [x] Update CHANGELOG.md with version entry
- [x] Write this spec doc

## Required review gates

- [ ] Architecture review
- [ ] Go standards code review
- [ ] API coverage review
- [ ] Security review
- [ ] Final HITL review

## Validation

- [x] `go test ./... -race`
- [x] `python3 scripts/validate_release_sync.py --upstream-root /tmp/openclaw --go-root . --version v2026.4.29`

## Status

- In progress

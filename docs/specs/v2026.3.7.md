# Upstream Sync Spec: v2026.3.7

- Upstream release: https://github.com/openclaw/openclaw/releases/tag/v2026.3.7
- Tracking issue: https://github.com/a3tai/openclaw-go/issues/21

## Relevant upstream deltas (initial triage)

- `sessions.get` is called out explicitly in upstream context-engine work and should be validated for schema/behavior parity in `protocol/` and `gateway/`.
- Gateway auth behavior changed to require explicit `gateway.auth.mode` when both token and password are set (breaking change).
- New/expanded gateway and tooling behavior around SecretRef, ACP bindings, topic routing, and config schema lookup are likely to affect typed gateway surface expectations.
- Upstream includes broad security hardening and plugin route/auth changes; review for any externally visible protocol/gateway payload implications.

## openclaw-go changes required

- [ ] Confirm `sessions.get` and related session payload types still match upstream wire shape.
- [ ] Re-run upstream method parity and resolve any drift surfaced after this release.
- [ ] Validate auth/config-related gateway request/response types tied to explicit auth mode behavior.
- [ ] Expand `gateway/methods_test.go` where upstream response shapes changed.
- [ ] Update `CHANGELOG.md` with v2026.3.7 sync notes.

## Required review gates

- [ ] Architecture review
- [ ] Go standards code review
- [ ] API coverage review
- [ ] Security review (Kingpin)
- [ ] Final HITL review

## Validation

- [ ] `go test ./... -race`
- [ ] `python3 scripts/upstream_drift_report.py --upstream-root /tmp/openclaw --go-root .`

## Status

- Draft
